name: CI Guards

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  guards:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run CI Guards
        shell: pwsh
        run: |
          pwsh -File tools/ci/run_guards.ps1 -Root .
