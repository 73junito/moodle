# === sandbox runtime outputs ===
sandbox_artifacts/
installer_extracted/
payload_out/
exported-sandbox-artifacts/

# === logs / temp ===
*.log
*.tmp
*.cache

# === CI runtime outputs ONLY ===
# These are runtime artifacts and must not be committed.
manifest-run-summary.json
manifest-run-diff.json
ci-decision.json
out.json
test-ci-decision.json
.github/artifacts/

# === run outputs (scoped) ===
**/output/
**/artifacts/

# === fixtures (always keep tracked fixtures)
!tools/fixtures/

# local OS VCS/temp
.DS_Store
Thumbs.db
