# === block massive dirs ===
sandbox_artifacts/
installer_extracted/
public/
payload_out/
exported-sandbox-artifacts/

# === generic data ===
*.csv
*.tsv
*.jsonl
*.zip
*.bin

# === logs / temp ===
*.log
*.tmp
*.cache

# === runs ===
# Runtime outputs only — keep fixtures/versioned inputs outside `runs/`.
# Ignore all generated run outputs under the runtime runs tree only.
tools/runs/
tools/runs/**/output/
tools/runs/**/artifacts/

# Keep versioned fixtures under tools/fixtures/

# local OS VCS/temp
.DS_Store
Thumbs.db
